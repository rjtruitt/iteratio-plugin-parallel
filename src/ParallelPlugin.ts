/**
 * Re-exports from the barrel for backward compatibility with direct imports.
 */
export {
  ParallelPlugin,
  ParallelConfig,
  ParallelOptions,
  ParallelResult,
  ParallelResultItem,
  createParallelPlugin,
} from './index';
